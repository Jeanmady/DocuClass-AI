# Data folders
data/raw/
data/processed/

# Model weights — safetensors are too large for git (>100 MB).
# They are hosted on Hugging Face Hub and downloaded automatically on first run.
# All other model artefacts (config, tokenizer, label encoder) are tracked normally.
models/docuclass_minilm/model.safetensors
models/docuclass_minilm/training_args.bin

# Training outputs and logs
outputs/training_logs/
outputs/checkpoints/
results/

# Python environment
.venv/
__pycache__/
*.pyc
*.egg-info/

# Environment variables (keep .env.example, ignore .env)
.env

# OS generated files
.DS_Store